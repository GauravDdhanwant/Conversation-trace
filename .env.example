# Optional — app works without this using rule-based fallback extraction
OPENAI_API_KEY=sk-your-key-here
OPENAI_MODEL=gpt-4o-mini

# SQLite database path (default: ./data/conversation-trace.db)
# DATABASE_PATH=./data/conversation-trace.db
